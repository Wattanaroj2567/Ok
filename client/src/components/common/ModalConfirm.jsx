import React from 'react';
import { Dialog, DialogTitle, DialogContent, DialogActions, Button } from '@mui/material';

// TODO (ModalConfirm): Frontend Developer – รับ props (open, title, onConfirm, onClose, children)